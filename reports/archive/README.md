# Archive

This directory is reserved for inactive local drafts and historical notes that are not part of the active delivery package.

Some files in this directory may be ignored by `.gitignore` because they were intentionally kept out of prior final commits.
